<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hackathon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".results.reswonActivity"></activity>
        <activity android:name=".results.reshumActivity" />
        <activity android:name=".results.rescapActivity" />
        <activity android:name=".results.rescurrActivity" />
        <activity android:name=".results.resgksActivity" />
        <activity android:name=".results.resanimActivity" />
        <activity android:name=".AnimalActivity" />
        <activity android:name=".gksActivity" />
        <activity android:name=".humActivity" />
        <activity android:name=".CapActivity" />
        <activity android:name=".CurrActivity" />
        <activity android:name=".AgriActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".lessonActivity" />
        <activity android:name=".wonderActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>